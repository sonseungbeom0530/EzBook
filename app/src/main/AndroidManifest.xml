<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ezbook">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <!-- google map -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".OrderDetailsAdminActivity" />
        <activity android:name=".ShopReviewsActivity" />
        <activity android:name=".WriteReviewActivity" />
        <activity android:name=".OrderDetailsUserActivity" />
        <activity
            android:name=".ShopDetailsActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".UserBookstoreActivity" />
        <activity android:name=".EditProductActivity" />
        <activity android:name=".AdminBookstoreActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".AdminProfileActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".RegisterAdminActivity" />
        <activity
            android:name=".ThereProfileActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddPostActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ChatDashboardActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" /> <!-- start from login -->

            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".EditUserProfileActivity" />
        <activity android:name=".EditAdminProfileActivity" />


    </application>

</manifest>